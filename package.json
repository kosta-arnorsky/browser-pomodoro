{
  "scripts": {
    "build": "npm install && node node-scripts/build.js",
    "inject-template": "npm install && node node-scripts/injectTemplate.js"
  },
  "devDependencies": {
    "minify": "^6.0.1"
  }
}
